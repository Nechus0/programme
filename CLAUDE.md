# Git workflow

- Auto-push: after creating a commit in this repo, push it to `origin` immediately without asking for confirmation first.
- Exceptions (always confirm before these): force-push, `git reset --hard`, rewriting/amending already-pushed commits, or pushing directly to a protected/production branch outside the normal flow.
